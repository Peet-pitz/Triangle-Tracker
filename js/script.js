var side, a = parseInt(prompt("Enter side a"));
var side, b = parseInt(prompt("Enter side b"));
var side, c = parseInt(prompt("Enter side c"));
        if (side, a === side, b === side, c) {
            alert ("Equilateral Triangle");
        }